# dependencies
node_modules/
.pnp.*

# env (never commit secrets; keep the examples)
.env
.env.*
!.env.example

# next.js
.next/
out/

# builds
dist/
build/

# foundry / solidity
artifacts/
cache/
typechain-types/
coverage/
out/
# Foundry deps are reinstalled via `forge install` (pinned in contracts/README) — don't commit 25MB of vendored OZ/forge-std
contracts/lib/
contracts/broadcast/

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# misc
.DS_Store
*.log
*.pem

# local AI-agent skill reference (celopedia) — not part of the MiniCeliq app
.agents/
skills-lock.json

# build-log (local brainstorm notes — never push to the public repo)
build-log/
